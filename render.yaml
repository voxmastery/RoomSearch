services:
  - type: web
    name: roomsearch
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: MOSS_PROJECT_ID
        sync: false
      - key: MOSS_PROJECT_KEY
        sync: false
